# OC 助手 PRD v3｜OC 创作工作台 × 酒馆内容生产后台

> 版本：v3 重构版  
> 更新时间：2026-05-22  
> 文档用途：给产品、设计、开发 Agent 使用，作为当前版本的统一 PRD。  
> 当前结论：重构为「OC 创作工作台 + 酒馆内容生产后台 + 可选插件回写入口」。

---

## 0. 本次重构结论

### 0.1 一句话定位

OC 助手是一套面向原创 OC 创作者、AI 酒馆玩家的创作工作台。

它以角色为锚点，以多角色共现、虚构时间轴、关系卡和创作档案为核心组织方式，把零散的人设、世界观、剧情片段、聊天记录和关系线整理成可继续创作、可导出、可交付、可用于 SillyTavern / 酒馆生态的内容资产。

### 0.2 当前版本的产品主线

```text
导入 / 创建 OC 素材
→ 建立项目与角色锚点
→ 挂载 Item
→ 梳理多角色共现与虚构时间轴
→ 形成创作档案
→ 生成 / 编辑关系卡、剧情线、世界观条目
→ 导出角色卡、世界书、开场白、互动场景、交付说明
→ 从酒馆聊天中回收记忆候选，确认后回写 Workbench
```

---

## 1. 当前开发进度整合

### 1.1 代码与部署现状

- 代码库路径：`/Users/weidian/oc-workbench`
- 当前开发分支：`.worktrees/mvp-p0/`
- 部署目标：Vercel
- 技术栈：
  - Next.js 16 App Router
  - React 19
  - Tailwind CSS 4
  - shadcn/ui
  - Prisma 7
  - PostgreSQL via Prisma Accelerate
  - OpenRouter API，当前模型倾向 `deepseek-chat-v3-2`
  - 认证待选：Supabase Auth / Clerk / NextAuth

### 1.2 已实现能力，原则上保留复用

Workbench 侧已有：

- Character 角色 CRUD；
- Item 类型系统：`profile / snippet / reference / image / state_card`；
- 多角色共现页 `/co-occurrence`；
- 共现时间轴 `/timeline`，已支持 dnd-kit 拖拽排序；
- 关系图谱 `/graph`，force-directed SVG；
- CharacterRelationship 模型；
- CharacterGroup 角色组；
- AI 导入基础能力：叙事解析、摘要生成、参考召回；
- 基础搜索：角色名 + Item 内容；
- 图片上传，目前是 base64 存数据库。
- OC Tavern Plugin MVP 已打通：从酒馆消息中提取记忆候选，用户审查后回写 Workbench / SillyTavern；
- 开始考虑 Electron 桌面打包，但当前主要产品链路仍应先稳定 Web / Workbench；
- 目前更重要的是收束产品闭环，而不是继续堆“看起来很全”的功能。

### 1.3 当前核心缺口

优先级最高的缺口不是“再做一个 AI 功能”，而是：

1. 用户认证系统缺失；
2. 数据没有 user_id 隔离，所有数据全局共享；
3. 已有功能没有围绕“项目 / 创作档案 / 资产包”重新串起来；
4. 关系图谱已经有，但轻量关系卡不够产品化；
5. 资产导出系统未开始；
6. AI 功能有基础版，但还没有统一的“候选 → 确认 → 写入”交互；
7. 插件 MVP 已有，但 Workbench 主体 PRD 还没有正式接住“聊天记忆回收 / 回写”这个扩展入口。

---

## 2. 产品边界

### 2.1 本产品是什么

OC 助手是一个内容生产后台，不是最终游玩平台。

它服务三类任务：

1. 整理 OC：把散乱资料整理为可持续维护的创作档案；
2. 生产资产：把创作档案导出为角色卡、世界书、开场白、互动场景、关系梳理、剧情线说明；
3. 回收记忆：从酒馆互动记录中提取可维护的信息，经过用户确认后回写到创作档案。

### 2.2 本产品不是什么

当前版本明确不做：

- 不做完整 AI 酒馆平台；
- 不做小手机角色卡场景支持；
- 不做“万能角色卡生成器”；
- 不做自动 OOC 审判器；
- 不做自动续写 / 代写正文作为主功能；
- 不做未授权 IP 商用；
- 不做二改、转售他人角色卡；
- 不做一开始就全自动理解所有剧情关系的黑箱系统；
- 不做复杂多人协作权限；
- 不做完整模板市场；
- 不做绕过平台规则的商业化工具。

### 2.3 商业化边界

前期服务口径应统一为：

> 原创 OC 设定整理 / 世界观梳理 / AI 互动角色卡结构化服务。

可做：

- 原创 OC；
- 客户自有世界观；
- 客户明确有权使用的素材；
- 私人使用场景下的整理与调试建议。

谨慎做：

- 同人 / 商业 IP，只能作为私人整理，不公开展示，不承诺商业用途。

不做：

- 未授权商业 IP 角色卡售卖；
- 改卖他人角色卡；
- 代理绕过平台规则；
- 宣称可用于任意平台商业化。

---

## 3. 目标用户与核心场景

### 3.1 用户类型 A：自用型 OC 创作者

特征：

- 有多个 OC；
- 角色关系复杂；
- 经常先写片段，再补设定；
- 人设、世界观、旧文散在 Notion、Obsidian、备忘录、文档、聊天记录中；
- 写多人剧情时容易忘记共同经历、关系阶段、前后顺序。

核心目标：

- 把角色立起来；
- 把旧内容导进来；
- 按角色、关系、剧情线重新组织；
- 回看共现时间线；
- 继续写新片段。

### 3.2 用户类型 B：酒馆玩家 / AI 互动叙事玩家

特征：

- 需要角色卡、世界书、开场白、示例对话；
- 希望角色稳定，不 OOC；
- 会在模型互动后产生新的设定、关系变化、记忆碎片；
- 需要把酒馆聊天结果回收到长期创作档案中。

核心目标：

- 从 OC 档案生成酒馆可用资产；
- 理解哪些内容放角色卡，哪些放世界书，哪些放开场白；
- 把互动中产生的新记忆回收；
- 长期维护角色一致性，如何写好角色卡。

---

## 4. 产品目标

### 4.1 P0 目标

P0 不再是“做一个完整 OC 平台”，而是：

> 修好数据隔离，串起最小创作 / 生产闭环，让用户能把混乱素材整理成可编辑的创作档案，并导出 Markdown 资产包初稿。

P0 要验证：

1. 用户能创建角色并挂载内容；
2. 用户能导入旧内容并归属到角色 / 多角色；
3. 用户能通过共现页和时间轴回看多人上下文；
4. 用户能建立轻量关系卡；
5. 用户能导出一份基础资产包；
6. 用户能接受 AI 作为“候选生成器”，而不是黑箱替用户判断。

### 4.2 P1 目标

P1 目标是提升生产效率：

1. AI 辅助从素材中提取角色、关系、事件、世界观条目；
2. 生成可编辑的角色卡字段、世界书条目、开场白候选；
3. 插件回写能力与 Workbench 主体稳定整合；
4. 角色组、状态卡、单角色时间轴可用；
5. 支持匿名案例导出，服务账号运营。

### 4.3 P2 目标

P2 目标是扩展为更完整的互动叙事资产管理系统：

1. 多平台导出；
2. 角色卡 V2 / 绑定世界书格式适配；
3. 完整关系图谱；
4. 分层时间轴；
5. 剧情线对象；
6. 世界观对象；
7. 账号展示 / 接单管理模块；
8. 更复杂的角色阶段快照和长期记忆管理。

---

## 5. 核心产品原则

### 5.1 轻建档

角色创建只要求最小信息：

- 角色名；
- 一句话备注可选；
- 头像可选。

不要求一开始填写完整角色卡、关系、世界观、属性表。

### 5.2 导入先归属，后整理

导入内容必须先挂给角色或角色组，不进入无主仓库。

默认导入为 `reference` 类型，原样保存。之后再拆分、改类型、补角色标签、打阶段标签。

### 5.3 共现优先于复杂图谱

多人创作中最先要解决的问题不是“关系图好不好看”，而是：

- 这些人共同经历了什么；
- 按什么虚构顺序发生；
- 当前阶段有哪些关键上下文。

因此共现页和共现时间轴仍然是主线。

### 5.4 关系卡进入主线，但图谱延后

关系梳理已经从“后续想法”升级为核心交付物。

P0 做轻量关系卡：

- A 对 B 的视角；
- B 对 A 的视角；
- 关系阶段；
- 共同事件；
- 核心矛盾；
- 行为边界；
- 可互动场景。

复杂可视化图谱留到 P2。

### 5.5 AI 只做候选，不做最终事实

AI 的所有结果都必须走：

```text
AI 提取候选
→ 用户确认 / 修改 / 忽略
→ 写入系统
```

适用于：

- 角色名；
- 关系描述；
- 关键事件；
- 世界书条目；
- 角色卡字段；
- 剧情顺序；
- 关系阶段；
- 记忆候选。

### 5.6 创作档案是母本

不要让产品直接从“用户粘贴素材”跳到“角色卡”。

中间必须有“创作档案”层，因为后续所有导出物都应该从同一母本生成。

创作档案包括：

- 角色基础信息；
- 核心矛盾；
- 行为模式；
- 说话方式；
- 关系卡；
- 共同事件；
- 世界观条目；
- 剧情线；
- 当前状态；
- 用户偏好与禁区；
- 可复用语气样本；
- 互动玩法建议。

---

## 6. 信息架构

### 6.1 Project 项目

新增 Project 是必要的。

原因：

- 一个用户可能有多个世界观；
- 接单模式下，一个客户就是一个项目；
- 资产包需要以项目为单位导出；
- 账号案例也需要项目化管理。

字段建议：

```ts
Project {
  id
  userId
  name
  description
  mode // self_use | commission | demo
  clientAlias
  targetPlatform // sillytavern | other | markdown_only
  createdAt
  updatedAt
}
```

### 6.2 Character 角色

```ts
Character {
  id
  userId
  projectId
  name
  note
  avatar
  createdAt
  updatedAt
}
```

### 6.3 Item 内容项

所有资料、片段、摘抄、状态、图片说明都先抽象为 Item。

```ts
Item {
  id
  userId
  projectId
  title
  content
  itemType
  linkedCharacters
  linkedRelationships
  linkedNarrativeLines
  sourceType // manual | import | tavern_chat | ai_candidate
  image
  pinned
  fictionalOrder
  fictionalStage
  isPublic
  createdAt
  updatedAt
}
```

Item Type：

- `profile`：设定资料；
- `snippet`：创作片段；
- `reference`：摘抄 / 导入原文 / 灵感；
- `image`：图片参考；
- `state_card`：当前状态卡；
- `relationship_note`：关系描述；
- `scene_seed`：互动场景种子；
- `opening`：开场白；
- `lore_entry`：世界书条目；
- `card_field`：角色卡字段草稿；
- `delivery_note`：交付说明；
- `tavern_memory`：从酒馆互动回收的记忆。

### 6.4 Relationship 关系卡

```ts
Relationship {
  id
  userId
  projectId
  characterAId
  characterBId
  relationshipType
  aToBDescription
  bToADescription
  currentStage
  coreTension
  sharedEvents
  secretsOrDebts
  boundaries
  triggerScenes
  createdAt
  updatedAt
}
```

关系卡必须支持不对等关系。例如：

```text
A → B：A 认为 B 是唯一可信任的人，但不愿承认依赖。
B → A：B 认为 A 是麻烦、责任和旧债，不认为这是一段亲密关系。
```

### 6.5 NarrativeLine 剧情线

```ts
NarrativeLine {
  id
  userId
  projectId
  name
  description
  linkedCharacters
  linkedItems
  color
  currentStatus
  createdAt
  updatedAt
}
```

P0 可先只做字段预留，P1/P2 再做完整页面。

### 6.6 WorldEntry 世界观条目

```ts
WorldEntry {
  id
  userId
  projectId
  title
  content
  keywords
  entryType // location | organization | rule | history | faction | object | concept
  priority
  linkedCharacters
  linkedItems
  createdAt
  updatedAt
}
```

P0 可先用 `lore_entry` 类型 Item 代替，P1 再拆独立对象。

### 6.7 DeliveryPackage 交付包

```ts
DeliveryPackage {
  id
  userId
  projectId
  name
  targetPlatform
  includedCharacters
  includedRelationships
  includedNarrativeLines
  includedWorldEntries
  deliverableTypes
  status // draft | exported | archived
  markdownContent
  createdAt
  updatedAt
}
```

### 6.8 TavernMemoryCandidate 酒馆记忆候选

插件链路需要一个候选层，不应直接写入角色档案。

```ts
TavernMemoryCandidate {
  id
  userId
  projectId
  sourceMessageId
  sourceChatId
  content
  suggestedType // profile | relationship_note | lore_entry | state_card | snippet
  suggestedCharacters
  confidence
  status // pending | accepted | edited | rejected
  createdAt
  updatedAt
}
```

---

## 7. 页面结构

### 7.1 首页：项目库 + 最近工作区

旧首页“角色库”需要升级为“项目库”。

页面目标：

- 进入已有项目；
- 新建项目；
- 快速导入内容；
- 查看最近更新角色和内容；
- 继续上次工作。

模块：

- 新建项目；
- 导入素材；
- 最近项目；
- 最近角色；
- 最近内容；
- 全局搜索。

### 7.2 项目页

项目页是新版主入口。

包含：

- 项目简介；
- 角色列表；
- 最近导入内容；
- 多角色共现入口；
- 时间轴入口；
- 关系卡入口；
- 世界观 / Lorebook 草稿入口；
- 交付包入口；
- 酒馆记忆候选入口。

### 7.3 角色页

保留旧角色页结构，但接入项目层。

包含：

- 角色基础信息；
- 当前状态卡；
- 资料 Tab；
- 创作 Tab；
- 关系 Tab；
- 世界书条目 Tab；
- 资产导出预览；
- 新增内容；
- 进入共现页；
- 查看关联记忆候选。

### 7.4 导入页

导入页是开门能力。

流程：

1. 选择项目；
2. 选择已有角色 / 新建角色 / 暂存为待识别；
3. 粘贴文本或上传文件；
4. 默认简单导入为 `reference`；
5. 可选 AI 辅助提取；
6. 导入后进入角色页 / 共现页 / 创作档案草稿页。

P0 只要求粘贴文本和 Markdown / txt。docx 可延后。

### 7.5 多角色共现页

核心问题：

> 这组角色共同关联了哪些内容？现在写他们之前应该看什么？

功能：

- 选择 2 个及以上角色；
- 展示同时关联这些角色的 Item；
- 可按类型筛选；
- 可按最近更新查看；
- 可跳转共现时间轴；
- 可新增挂给当前角色组的创作片段；
- 可从共现内容生成关系卡草稿。

### 7.6 共现时间轴

核心问题：

> 这群人一起经历了什么？按什么虚构顺序发生？

功能：

- 选择一个角色：单角色时间轴；
- 选择多个角色：共现时间轴；
- 按 `fictionalOrder` 排序；
- 支持拖拽排序；
- 支持阶段标签；
- 支持从时间轴进入相关 Item / 关系卡 / 剧情线。

### 7.7 关系卡页

P0 新增。

页面内容：

- 关系双方；
- A → B 视角；
- B → A 视角；
- 关系类型；
- 当前阶段；
- 核心矛盾；
- 共同事件；
- 秘密 / 亏欠 / 误解；
- 互动边界；
- 可触发场景；
- 导出为关系说明。

### 7.8 创作档案页

这是新版新增的中间层。

作用：

- 聚合某项目下的角色、关系、世界观、剧情线、状态卡；
- 作为角色卡 / 世界书 / 开场白 / 互动场景的母本；
- 支持人工编辑；
- 支持 AI 生成草稿后逐项确认。

结构：

```text
项目概览
/ 角色档案
/ 关系卡
/ 世界观条目
/ 关键事件
/ 剧情线
/ 当前状态
/ 用户偏好与禁区
/ 可复用语气样本
/ 待追问问题
```

### 7.9 交付包导出页

P0 做 Markdown 模板导出，不急着做复杂平台格式。

用户选择：

- 目标平台：Markdown / SillyTavern 草稿 / 通用酒馆；
- 角色；
- 关系；
- 世界观条目；
- 剧情线；
- 输出类型。

输出：

- 角色卡草稿；
- 世界书草稿；
- 开场白候选；
- 互动场景候选；
- 关系梳理；
- 剧情线梳理；
- 交付说明。

### 7.10 酒馆记忆候选页

承接 OC Tavern Plugin。

页面目标：

- 展示从酒馆消息提取出来的候选记忆；
- 让用户逐条确认、编辑、忽略；
- 写入对应角色、关系、世界观或状态卡。

状态：

- 待确认；
- 已接受；
- 已编辑；
- 已拒绝。

---

## 8. 核心用户流程

### 8.1 自用创作流程

```text
新建项目
→ 新建角色
→ 添加设定 / 片段 / 图片 / 摘抄
→ 选择多个角色进入共现页
→ 在时间轴中整理共同事件
→ 写新片段
→ 新片段继续挂回角色和关系
```

### 8.2 导入旧内容流程

```text
进入项目
→ 点击导入
→ 选择角色或新建角色
→ 粘贴旧文 / 设定 / 聊天记录
→ 默认保存为 reference
→ 可选 AI 提取候选
→ 用户确认角色 / 关系 / 事件 / 世界观条目
→ 进入创作档案页
```

### 8.3 酒馆记忆回收流程

```text
用户在 SillyTavern 中选择消息
→ 插件提取记忆候选
→ 候选进入 Workbench
→ 用户逐条审查
→ 接受 / 编辑 / 拒绝
→ 写入角色档案、关系卡、世界观条目或状态卡
```

---

## 9. P0 / P1 / P2 范围

### 9.1 P0：当前必须完成的最小可用版本

P0 目标：支撑“自用整理 + 小规模接单 + Markdown 资产包导出”。

#### P0.0 工程地基

- 接入第三方认证；
- 所有模型增加 `userId`；
- 所有查询按 `userId` 过滤；
- 迁移已有数据结构；
- 保证用户只能访问自己的项目、角色、Item、关系、导出包。

#### P0.1 项目与角色管理

- 项目库；
- 新建项目；
- 角色 CRUD；
- 角色列表；
- 角色详情页；
- CharacterGroup 保留。

#### P0.2 Item 内容系统

- 创建 Item；
- 编辑 Item；
- 删除 Item；
- 类型选择；
- 关联角色；
- 置顶；
- 虚构顺序；
- 阶段标签；
- 简单搜索；
- 图片上传先保留，但后续应从 base64 改为对象存储。

#### P0.3 导入系统

- 粘贴文本导入；
- Markdown / txt 导入；
- 导入前选择角色；
- 默认保存为 reference；
- 导入后跳转角色页或共现页；
- AI 辅助导入入口可以出现，但不作为 P0 依赖。

#### P0.4 多角色共现 + 时间轴整合

- 共现页接入项目和用户隔离；
- 时间轴接入项目和用户隔离；
- 拖拽排序稳定；
- 阶段标签可编辑；
- 新增片段自动关联当前角色集合。

#### P0.5 轻量关系卡

- 创建关系卡；
- 编辑 A → B / B → A；
- 关联共同事件；
- 关联 Item；
- 从共现页进入关系卡；
- 关系卡可导出 Markdown。

#### P0.6 Markdown 交付包导出

P0 只做 Markdown，不做复杂格式。

支持导出：

- 角色卡草稿；
- 世界书草稿；
- 关系梳理；
- 开场白候选；
- 互动场景候选；
- 剧情线说明；
- 交付说明。

#### P0.7 酒馆记忆候选接入

如果插件 MVP 已完成，Workbench P0 至少要能接住候选：

- 候选列表；
- 候选详情；
- 接受 / 编辑 / 拒绝；
- 写入 Item / Relationship / State Card。

### 9.2 P1：生产效率增强

- AI 从导入内容识别角色；
- AI 提取关系候选；
- AI 提取关键事件；
- AI 提取世界观条目；
- AI 生成角色卡字段草稿；
- AI 生成世界书条目草稿；
- AI 生成开场白候选；
- AI 生成互动场景候选；
- 当前状态卡增强；
- 角色组快速切换；
- 单角色时间轴；
- 匿名案例页；
- 公开分享页；
- 插件与 Workbench 双向同步更稳定。

### 9.3 P2：完整资产系统

- 完整关系图谱；
- 分层时间轴；
- 独立 NarrativeLine 页面；
- 独立 WorldEntry 页面；
- 多平台导出格式；
- Character Card V2 / Lorebook 格式适配；
- 酒馆导入包；
- 接单管理模块；
- 模板库；
- 多轮调试记录；
- 角色稳定性测试集。

---

## 10. AI 能力设计

### 10.1 AI 能做什么

用户主动触发后，AI 可以：

- 从素材中识别角色；
- 提取关系候选；
- 提取关键事件；
- 提取世界观条目；
- 将散文设定转为结构化角色档案；
- 将角色档案转为角色卡字段；
- 将世界观资料转为世界书条目；
- 将关系描述转为双向关系卡；
- 将剧情片段整理成剧情线；
- 生成开场白候选；
- 生成互动场景候选；
- 生成交付说明；
- 生成匿名案例文案；
- 从酒馆聊天中提取记忆候选。

### 10.2 AI 不能做什么

AI 不应默认：

- 自动读取用户全部文本；
- 自动判断用户是否 OOC；
- 自动改写用户创作；
- 自动覆盖用户档案；
- 自动生成不可追溯的最终交付物；
- 将用户内容用于训练。

### 10.3 AI 交互统一模式

所有 AI 功能统一使用候选卡片：

```text
候选标题
候选内容
建议写入位置
关联角色
关联关系 / 剧情线
来源文本
用户操作：接受 / 编辑后接受 / 忽略
```

---

## 11. 交付包模板

### 11.1 角色卡草稿

```text
# 角色卡：{角色名}

## 基础信息
- 名字：
- 一句话定位：
- 当前阶段：

## 角色描述
{description}

## 性格与行为模式
{personality}

## 说话方式
{speaking_style}

## 与用户 / 其他角色关系
{relationship_to_user_or_characters}

## 场景设定
{scenario}

## 开场白
{first_message}

## 示例对话
{example_dialogue}

## 行为规则
{behavior_rules}

## 避免事项
{boundaries}
```

### 11.2 世界书草稿

```text
# 世界书 / Lorebook

## 条目：{title}
- 关键词：
- 类型：
- 优先级：
- 内容：
- 使用说明：
```

### 11.3 关系梳理

```text
# 关系卡：{A} × {B}

## A 对 B
{a_to_b}

## B 对 A
{b_to_a}

## 关系阶段
{current_stage}

## 核心矛盾
{core_tension}

## 共同事件
{shared_events}

## 秘密 / 亏欠 / 误解
{secrets_or_debts}

## 互动边界
{boundaries}

## 可互动场景
{trigger_scenes}
```

### 11.4 交付说明

```text
# 使用说明

这份资产包包含：
- 角色卡草稿
- 世界书条目
- 关系梳理
- 开场白
- 互动场景
- 剧情线说明

建议使用方式：
1. 先导入角色卡；
2. 再按需添加世界书；
3. 根据当前关系阶段选择开场白；
4. 如果角色跑偏，优先检查：行为规则、示例对话、关系阶段、世界书触发关键词。
```

---

## 12. 验收标准

### 12.1 P0 工程验收

- 用户必须登录后才能访问数据；
- 不同用户之间的数据不可互相访问；
- 所有核心模型都带 `userId`；
- 旧页面接入认证后不崩；
- Vercel 部署可用。

### 12.2 项目 / 角色验收

- 用户可以新建项目；
- 用户可以在项目下创建角色；
- 创建角色只要求角色名；
- 创建后进入角色页；
- 角色可编辑、删除；
- 角色列表按更新时间排序。

### 12.3 导入验收

- 用户导入前必须选择项目；
- 用户可选择已有角色或新建角色；
- 粘贴文本可保存为 reference；
- 导入后内容出现在对应角色页 / 共现页；
- 不强制用户立刻拆分内容。

### 12.4 共现页验收

- 用户选择 2 个及以上角色后进入共现页；
- 共现页展示同时关联这些角色的内容；
- 用户可在共现页新增 snippet；
- 新 snippet 默认关联当前角色集合。

### 12.5 时间轴验收

- 时间轴按 fictionalOrder 排序；
- 未排序内容有明确处理方式；
- 用户可拖拽排序；
- 用户可编辑 fictionalStage；
- 排序结果保存后刷新仍然存在。

### 12.6 关系卡验收

- 用户可在项目内新建关系卡；
- 关系卡支持 A→B 与 B→A 两个方向；
- 关系卡可关联 Item；
- 关系卡可从共现页创建；
- 关系卡可导出 Markdown。

### 12.7 资产包验收

- 用户可选择角色、关系、世界观条目生成 Markdown；
- Markdown 至少包含角色卡、关系梳理、开场白候选、交付说明；
- 用户可复制或下载 Markdown；
- 导出内容来自用户已确认内容，不凭空生成最终事实。

### 12.8 AI 候选验收

- AI 功能必须由用户主动点击；
- AI 结果以候选卡展示；
- 用户可以接受、编辑、忽略；
- 未确认内容不能写入正式档案；
- 候选保留来源片段，便于追溯。

---

## 13. 指标体系

### 13.1 产品使用指标

- 新用户创建第一个项目用时；
- 新用户创建第一个角色用时；
- 创建角色后 24 小时内添加 Item 比例；
- 首周导入功能使用率；
- 多角色共现页访问率；
- 时间轴排序 / 打阶段标签比例；
- 关系卡创建率；
- 资产包导出率。

### 13.2 生产效率指标

- 从素材导入到创作档案草稿的平均用时；
- AI 候选接受率；
- AI 候选编辑后接受率；
- AI 候选忽略率；
- 单个项目平均导出资产包数量；
- 单个项目平均关系卡数量；
- 交付包二次编辑比例。

### 13.3 接单 / 账号验证指标

- 匿名案例生成数量；
- 案例内容发布数量；
- 咨询转化数量；
- 免费 / 低价内测完成数量；
- 交付物满意度；
- 用户是否愿意继续维护同一项目；
- 用户是否愿意为完整资产包付费。

---

## 14. 版本开发顺序建议

### 第一步：别再扩功能，先修认证和数据隔离

开发任务：

- 选择认证方案；
- 接入登录 / 注册；
- 所有表加 userId；
- 所有 API 加用户过滤；
- 修复全局共享数据问题；
- 完成 Vercel 环境变量配置。

这是当前最大阻塞。

### 第二步：加入 Project 层

开发任务：

- 新建 Project 模型；
- 首页改为项目库；
- Character / Item / Relationship 关联 projectId；
- 已有页面都进入项目上下文。

### 第三步：整合已有功能，而不是重写

开发任务：

- 角色 CRUD 接 Project / User；
- 共现页接 Project / User；
- 时间轴接 Project / User；
- 关系图谱先保留，但不作为主入口；
- 基础搜索限定在当前用户 / 项目内。

### 第四步：做轻量关系卡

开发任务：

- Relationship 页面；
- 双向描述；
- 关联 Item；
- 从共现页一键创建关系卡；
- Markdown 导出。

### 第五步：做 Markdown 资产包

开发任务：

- DeliveryPackage 模型；
- 交付包生成页面；
- 选择角色 / 关系 / Item；
- 套模板生成 Markdown；
- 支持复制 / 下载。

### 第六步：接入插件候选

开发任务：

- TavernMemoryCandidate 模型；
- 候选列表页；
- 接受 / 编辑 / 忽略；
- 写入 Item / Relationship / state_card；
- 标记来源。

### 第七步：再做 AI 提效

开发任务：

- 导入内容提取候选；
- 角色卡字段草稿；
- 世界书条目草稿；
- 开场白候选；
- 互动场景候选；
- 匿名案例文案。

---

## 15. 给开发 Agent 的执行口径

当前不要让 Agent 继续“凭感觉优化页面”。

应该给它以下任务：

```text
你现在不是从零开发 OC 助手，而是在已有 OC Workbench 基础上重构 PRD v3 的 P0。

优先级：
1. 接入认证与 userId 数据隔离；
2. 加 Project 层，把角色、Item、关系、时间轴都挂到项目下；
3. 保留并整合已有角色 CRUD、Item、共现页、时间轴、关系图谱、AI 导入基础能力；
4. 新增轻量关系卡页面；
5. 新增 Markdown 资产包导出；
6. 接入 TavernMemoryCandidate 候选确认页；
7. 所有 AI 输出必须是候选，不允许自动写入正式档案。

不要做：
- 完整 AI 酒馆平台；
- 小手机角色卡支持；
- 自动 OOC 审查；
- 自动续写正文；
- 复杂模板市场；
- 未授权 IP 商用功能；
- 不经用户确认的后台分析。
```

---

## 16. 当前最小可执行版本定义

### 16.1 输入

用户可以输入：

- 角色设定；
- 世界观；
- 剧情片段；
- 关系描述；
- 酒馆聊天片段；
- 客户素材；
- 图片参考说明。

### 16.2 处理

系统支持：

- 创建项目；
- 创建角色；
- 导入内容；
- 挂载角色；
- 梳理共现；
- 整理时间轴；
- 创建关系卡；
- 生成创作档案；
- 接受 / 编辑 AI 候选；
- 整理成资产包。

### 16.3 输出

系统至少能导出 Markdown：

```text
/角色卡草稿
/世界书草稿
/关系梳理
/开场白候选
/互动场景候选
/剧情线梳理
/交付说明
```

### 16.4 成功标准

P0 成功不是“功能很多”，而是：

- 能安全保存用户自己的项目；
- 能把混乱 OC 素材整理成清晰结构；
- 能支撑一次真实接单或自用整理；
- 能导出一份可编辑的资产包；
- 能把酒馆互动中产生的新记忆回收到创作档案；
- 用户不需要理解底层模型，也能知道每条内容应该放在哪里。

---

## 17. 当前版本一句话总结

OC 助手 v3 是一个面向 OC 创作者与 AI 酒馆内容生产场景的创作工作台。它不是酒馆平台，也不是单纯角色卡生成器，而是把角色、关系、剧情、世界观、互动记忆和交付物统一管理起来的内容生产后台。P0 的重点不是继续扩张，而是先完成用户隔离、项目化、关系卡、Markdown 资产包和酒馆记忆候选回写，跑通“混乱素材 → 创作档案 → 酒馆资产包”的最小闭环。
