# OC 创作工作台（一期 MVP）产品文档 v2

> 产品阶段：一期 MVP  
> 产品定位：面向 OC 创作者的非 AI 创作整理与创作辅助工具  
> 核心目标：围绕**同一世界观下的一组角色**建立创作工作台，既支持用户**从 0 开始创建角色并积累资料**，也支持用户通过**导入旧内容**快速进入继续创作状态。  
> 本版文档基于前一版方案继续收束，保留后续迭代方向，但明确一期的主闭环、优先级与边界。:contentReference[oaicite:0]{index=0}

---

# 1. 产品定位

## 1.1 产品是什么
一款服务于 OC 创作者的创作工作台。

它不是传统意义上的笔记管理工具，也不是 AI 代写器。  
它解决的是两类高频起点：

- 用户脑中已有角色或零散资料，但没有合适的地方把它们挂起来、整理起来、继续写下去
- 用户已有旧内容散落在其他工具中，希望导入后，围绕角色和多人剧情继续创作

## 1.2 一期的产品本质
一期的本质不是“搭建完整世界观系统”，而是：

**让用户围绕角色与多角色共现场景，快速建立可持续创作的工作台。**

## 1.3 一期不做什么
一期明确不做以下能力作为主闭环依赖：

- 不使用 AI 在后台持续分析用户私人创作文本
- 不自动判断角色当前阶段、关系阶段、剧情阶段
- 不做 OOC 自动检查
- 不做 AI 自动续写、代写、总结正文
- 不做复杂世界观对象系统
- 不做复杂权限与多人协作
- 不做完整关系图谱作为一期核心路径
- 不做完整全局叙事管理系统作为一期核心路径

---

# 2. 用户与问题

## 2.1 目标用户
有多个 OC、角色关系复杂、剧情推进非线性、经常先写片段后补设定，且同一世界观下存在多人互动与剧情交叉的创作者。:contentReference[oaicite:1]{index=1}

## 2.2 用户的两类核心进入场景

### 场景 A：从 0 开始
- 我有一个新角色或一组新角色
- 我脑中已经有一点设想，但还没整理
- 我希望先立起角色锚点
- 我希望慢慢把设定、摘抄、图和片段挂进去
- 我不想先填完整档案，想边整理边写

### 场景 B：导入进入
- 我在别处已经有笔记、摘抄、设定或旧文
- 我想把旧内容带进来
- 我希望先挂给相关角色，再慢慢拆和整理
- 我最终想落到一个可以继续写的工作台，而不是一个导入仓库

## 2.3 当前痛点

### 冷启动痛点
- 新角色出现时，不知道该从哪里开始
- 如果创建门槛太高，会直接放弃
- 很多工具要求先建完整档案，再开始写

### 旧内容迁移痛点
- 旧内容散落在 Notion、Obsidian、备忘录、文档中
- 不想先做复杂整理，只想先导进来再慢慢修
- 导入后如果不能快速落到角色和剧情上，迁移就没有意义

### 继续创作痛点
- 多角色场景写作时，上下文最容易断
- 围绕同一组角色的资料和旧片段分散，反复翻找很累
- 真正写的时候更需要“共同经历了什么、前后顺序是什么”，而不是抽象的关系描述
- 资料整理和创作分离，切换成本高

---

# 3. 产品目标

一期只验证三件事：

## 3.1 目标一：支持从 0 开始建立角色锚点
用户能以非常低的门槛创建角色，并立即开始往角色下挂内容。

## 3.2 目标二：支持通过导入进入创作状态
用户能先选择角色归属，再导入旧内容，并把这些内容纳入后续创作工作台。

## 3.3 目标三：支持围绕多人共现场景继续创作
用户能查看同一组角色的共同内容，并按虚构顺序衔接剧情，减少多人创作场景中的断裂感。

---

# 4. 产品原则

## 4.1 角色优先，但服务于多人剧情
所有内容优先围绕角色组织，但一期最强的创作视角不是单角色，而是**多角色共现**。

## 4.2 轻建档
新角色只需最少信息即可创建，不要求完整角色卡。

## 4.3 导入先归属，后整理
导入不是先丢到无主仓库，而是先挂给角色，再慢慢拆分、补标签、改类型。

## 4.4 共现优先于图谱
一期更优先解决“这些角色共同经历了什么、按什么顺序发生”，而不是先做复杂关系可视化。

## 4.5 时间顺序优先于关系抽象
对于创作辅助来说，剧情衔接和虚构顺序比关系图更高频、更刚需。

## 4.6 创作与整理同场发生
角色页与共现页既是资料回看页，也是继续创作页。

## 4.7 AI 默认关闭，仅主动触发
AI 只在用户主动点击时介入，不在后台持续分析，不默认读取当前私人文本。

## 4.8 隐私优先
用户文本用于存储、检索、展示和手动组织，不用于模型训练。

---

# 5. 一期核心闭环

一期最小闭环如下：

**创建角色锚点 / 导入旧内容 → 挂到角色 → 进入角色页或多角色共现页 → 按共现时间线回看上下文 → 继续写新片段 → 新内容继续挂回角色体系**

这个闭环同时覆盖：
- 从 0 开始
- 导入进入
- 继续创作
- 多角色剧情衔接

---

# 6. 一期主骨架

一期虽然有多个入口，但只有一个主骨架：

## 6.1 主骨架定义
**角色工作台 + 多角色共现页 + 共现时间轴**

### 角色工作台负责
- 从 0 建立角色
- 挂载资料与片段
- 单角色维度的整理与继续写

### 多角色共现页负责
- 围绕同一组角色聚合上下文
- 成为多人剧情创作的主场

### 共现时间轴负责
- 以虚构顺序组织共同事件
- 支撑多人剧情的前后衔接

---

# 7. 信息架构

## 7.1 核心对象

### Character（角色）
角色是一切内容的基础锚点。  
用户可以从 0 创建角色，也可以在导入时先选择已有角色作为归属。

### Item（内容项）
一期中，所有挂载到角色下的内容统一抽象为 Item。  
先不做过细对象拆分，避免冷启动和导入成本过高。

### Event View（多角色共现视图）
支持围绕 2 个及以上角色查看共同关联内容并继续创作。  
这是一期开创作价值最强的视图之一。

### Shared Timeline（共现时间轴）
围绕所选角色集合，以虚构顺序查看相关内容的时间轴视图。  
重点不是“看一个角色的一生”，而是“看这群人在同一世界观下共同经历了什么”。

### Character Public Card（角色公开卡）
从角色已有 profile 内容中，用户手动标记“对外可见”的条目，系统聚合为可分享页面。  
保留在方案中，但不作为 P0 主闭环依赖。

---

# 8. 数据模型建议

## 8.1 Character
字段建议：

- `id`
- `name`：角色名，必填
- `note`：一句话备注，可选
- `avatar`：角色图，可选
- `created_at`
- `updated_at`

## 8.2 Item
字段建议：

- `id`
- `content`
- `item_type`
- `linked_characters`：角色 ID 数组，支持 1 个及以上
- `created_at`
- `updated_at`
- `pinned`：是否置顶
- `title`：可选标题
- `image`：可选图片附件
- `fictional_order`：虚构顺序权重（整数，用于拖拽排序）
- `fictional_stage`：虚构阶段标签（可选文字，如"早期""转折后""第二章前后"）
- `is_public`：是否标记对外可见（仅 profile 类型生效）
- `branch`：叙事分支标签（可选字符串，P1 预留；null 表示主线，非 null 表示 IF线/AU/平行线，如 `"if线-A与B相遇"`）

## 8.3 Item Type
一期建议保留 5 类：

- `profile`：设定资料
- `snippet`：创作片段
- `reference`：摘抄 / 参考 / 灵感来源
- `image`：图片参考
- `state_card`：当前状态卡

说明：

- `profile`：角色设定、背景信息、阶段备注等
- `snippet`：对话、场景、剧情片段、关系片段、未完成文字
- `reference`：摘抄、外部参考、灵感来源
- `image`：角色图、氛围图、参考图
- `state_card`：当前状态卡，用于记录角色当前的性格关键词、所处阶段、与其他角色的关系现状。每个角色只有一个当前状态卡，用户手动���护更新

## 8.4 NarrativeLine（剧情线）
字段建议：

- `id`
- `name`：剧情线名称（如"主线""A与B的感情线""复仇线"）
- `description`：剧情线描述
- `linked_characters`：关联角色 ID 数组
- `color`：用于可视化区分的颜色标记
- `created_at`
- `updated_at`

说明：
- 剧情线对象用于管理多条并行剧情线
- Item 可以通过 `linked_narrative_lines` 字段关联到一个或多个剧情线
- 一期作为 P2 后续迭代方向，数据模型先预留

## 8.5 CharacterGroup（角色组）
字段建议：

- `id`
- `name`：角色组名称（如"主角组""反派阵营""第二章核心角色"）
- `character_ids`：角色 ID 数组
- `created_at`
- `updated_at`

说明：
- 角色组用于保存常用的角色组合，方便快速进入多角色共现页
- 用户可以在多角色共现页中将当前选择的角色组合保存为角色组
- 一期作为 P1 增强能力

---

# 9. 页面结构

## 9.1 首页 / 角色库

### 页面目标
同时承接两类入口：
- 通过导入进入
- 从 0 开始创建角色

### 页面优先级
**主按钮：导入内容**  
**次按钮：新建角色**

### 页面内容
- 导入内容按钮
- 新建角色按钮
- 角色列表
- 最近更新角色
- 最近更新内容
- 搜索框

### 首页上的用户动作
- 点击导入内容
- 点击新建角色
- 点击已有角色进入角色页
- 搜索角色或内容
- 从最近更新中返回继续写

---

## 9.2 新建角色页 / 弹窗

### 页面目标
降低冷启动门槛，让用户快速立一个角色锚点。

### 必填字段
- 角色名

### 选填字段
- 一句话备注
- 头像 / 参考图

### 设计要求
- 不要求填写完整角色卡
- 不要求填写关系、时间线、世界观、属性表
- 创建完成后立即进入角色页

---

## 9.3 导入页

### 页面目标
作为产品的开门能力，让用户把旧内容带进来，并立刻进入角色工作台。

### 实际实现（P0 已完成）

页面为两个 Tab 的导入表单，**不需要提前选角色**：

**Tab 1：内容导入**
- 格式：每行 `角色A,角色B\t内容`（Tab 分隔角色列表与内容）
- 或使用 `#角色名` 标签标注所属角色
- 批量解析后按行创建 Item，自动关联对应角色

**Tab 2：剧情导入（AI 解析）**
- 用户粘贴自由格式剧情文本
- 后端调用 OpenRouter/DeepSeek 一次性解析
- AI 识别角色名 + 拆分片段 + 标注关联角色
- 角色不存在则自动创建，片段直接写入为 `snippet` 类型 Item
- 无需用户逐步确认，一键完成

### 后续整理入口
导入后用户可以再做：
- 拆分成多个条目
- 修改内容类型
- 补角色标签
- 调整虚构顺序
- 加阶段标签

---

## 9.4 角色页

### 页面目标
角色页是单角色维度的工作台。  
用于从 0 建立角色、补资料、继续写、回看内容。

### 页面结构

#### A. 顶部：角色基础区
显示：
- 角色名
- 一句话备注
- 头像 / 角色图
- 编辑角色按钮

#### B. 中部：内容切换区
建议分为两个主要 Tab：

##### Tab 1：资料
用于展示：
- 设定资料
- 摘抄 / 参考
- 图片参考

##### Tab 2：创作
用于展示：
- 创作片段
- 最近补写内容
- 可继续写的内容

#### C. 页面操作区
提供：
- 新增资料
- 新增创作片段
- 上传图片
- 搜索当前角色下内容
- 手动置顶关键内容
- 进入多角色共现页的入口

### 页面价值
- 让角色从空白逐渐长完整
- 作为单角色的整理与继续写入口
- 为多角色创作提供锚点

---

## 9.5 多角色共现页

### 页面目标
支持用户围绕 2 个及以上角色查看共同内容并继续创作。  
这是一期的主创作视图之一。

### 进入方式
- 从角色页选择“与其他角色一起查看”，可多选
- 从搜索中直接选择多个角色
- 从某条挂了多个角色的内容进入

### 页面展示内容
- 同时关联所选角色的所有资料与创作片段
- 支持按最近更新查看
- 支持切换到共现时间轴查看
- 支持直接新增“挂给所选角色”的创作片段

### 页面价值
- 写多人场景时，能在同一处看到所有相关上下文
- 减少在多个角色页之间来回跳转
- 成为同一世界观下“这群人”的创作工作台

### 一期边界
- 不做结构化关系类型分类
- 不做关系自动总结
- 不做复杂关系状态系统

---

## 9.6 共现时间轴

### 页面目标
围绕当前所选角色集合，按虚构顺序查看共同内容。
这是一期优先级高于关系图谱的时间能力。

### 页面结构
共现时间轴与单角色时间轴共用同一个时间轴页面。

通过角色选择器切换视图：
- 选择 1 个角色：显示该角色的单角色时间轴
- 选择 2 个及以上角色：显示这些角色的共现时间轴

### 排序机制
两种方式并存：

#### 1. 拖拽排序
用户手动拖动内容调整相对顺序，系统记录 `fictional_order`

#### 2. 阶段标签
用户可给内容打模糊时间标签，如：
- 早期
- 初遇后
- 第二章前后
- 决裂前夜
- 转折后

### 页面展示内容
- 当前角色集合共同关联的内容
- 按 `fictional_order` 排列
- 按 `fictional_stage` 辅助分组

### 页面价值
- 帮助用户理解"这群人一起经历了什么"
- 支撑多人剧情场景的前后衔接
- 支持非线性创作习惯：先写 B，再补 A，再重新排序

### 一期边界
- 不做完整全局叙事线系统
- 不做自动时间推断
- 不做复杂依赖关系可视化

---

## 9.7 单角色时间线

### 页面目标
让用户以虚构故事时间为轴，查看某个角色经历的所有内容。

### 当前定位
这是非常有价值的能力，但优先级低于共现时间轴。  
保留在一期增强中，不从方案中删除。

---

## 9.8 角色公开分享页

### 页面目标
为约稿等场景提供干净的角色设定展示页，不要求用户重新填写。

### 内容来源
用户在角色页的 profile 内容中，将部分条目标记为“对外可见”。

### 当前定位
保留在一期增强中，不作为 P0 主闭环依赖。

---

## 9.9 关系图谱

### 页面目标
可视化展示角色之间的关系网络，帮助用户理清复杂关系，也可用于未来展示。

### 关系描述数据来源
关系图谱采用双向视角展示：

当角色 A 的 profile 中描述了与角色 B 的关系，角色 B 的 profile 中也描述了与角色 A 的关系时：
- 图谱中显示双向箭头
- 分别展示两个角色各自的视角描述
- 例如：A→B："A 认为 B 是朋友"，B→A："B 认为 A 是敌人"

这种设计支持角色之间关系认知的不对等，更符合复杂剧情中的真实情况。

### 当前定位
保留为后续迭代方向。
一期优先做时间轴，不优先做图谱。

---

## 9.10 分层时间轴

### 页面目标
以故事时间为横轴，以不同叙事线为纵轴，展示整个 OC 宇宙的剧情推进全貌。

### 当前定位
保留为后续迭代方向。  
一期先做共现时间轴，不直接做全局叙事层时间轴。

---

# 10. 关键用户流程

## 10.1 从 0 开始流程

### Step 1
用户进入首页，点击“新建角色”。

### Step 2
输入：
- 角色名
- 一句话备注（可选）
- 角色图（可选）

### Step 3
进入角色页。

### Step 4
用户开始往角色下添加内容：
- 一段设定
- 一句摘抄
- 一张图片
- 一个片段

### Step 5
用户围绕该角色继续写，或从角色页进入多角色共现页。

### 该流程的目标
让用户不需要先把角色整理完整，也能进入可创作状态。

---

## 10.2 导入进入流程

### Step 1
用户进入首页，点击“导入内容”。

### Step 2
先选择挂给哪个角色 / 哪些角色。

### Step 3
导入 Markdown / 文本 / 粘贴内容。

### Step 4
系统默认将内容原样保存为 `reference` 类型，并挂到刚才选择的角色体系下。

### Step 5
用户进入角色页或多角色共现页，开始回看与继续写。

### Step 6
后续再按需拆分、补标签、改类型、排时间顺序。

### 该流程的目标
降低迁移门槛，让用户先把旧内容带进来，再进入创作工作台。

---

## 10.3 多角色继续创作流程

### Step 1
用户从角色页或搜索中选择多个角色。

### Step 2
进入多角色共现页。

### Step 3
查看这些角色共同关联的旧内容。

### Step 4
切到共现时间轴，按虚构顺序回看共同经历。

### Step 5
点击“新增创作片段”。

### Step 6
新片段自动同时挂载到所选角色名下。

### 该流程的目标
让同一世界观下的多人剧情创作有稳定的上下文聚合点。

---

# 11. 功能设计细化

## 11.1 新增内容

### 新增资料
用途：
- 录入角色设定
- 保存摘抄
- 保存参考说明
- 记录某阶段信息

### 新增创作片段
用途：
- 写对话
- 写场景
- 写剧情片段
- 写关系推进
- 写未完成脑洞

### 设计要求
新增时只要求最少动作：
- 输入内容
- 选择挂给哪个角色（从角色页/共现页进入时默认已选）
- 选择内容类型

不要要求：
- 填大量字段
- 先做完整分类
- 填具体日期
- 定义剧情阶段

---

## 11.2 内容展示

### 资料 Tab 中显示
- `profile`
- `reference`
- `image`

### 创作 Tab 中显示
- `snippet`

### 排序方式
一期默认支持：
- 按最近更新排序
- 手动置顶
- 在时间轴中按虚构顺序排序

---

## 11.3 搜索

### 基础搜索
一期至少支持：
- 按角色名搜索
- 按内容关键词搜索
- 在单个角色页内搜索
- 在共现页内搜索当前角色集合相关内容

### 语义搜索
保留为主动触发的增强能力，不作为默认依赖。

---

## 11.4 手动置顶
用户可手动置顶关键内容，作为当前最重要的上下文。

适合置顶的内容包括：
- 当前阶段核心设定
- 最常用参考摘抄
- 关系关键片段
- 最近写到一半的片段

### 置顶意义
在不依赖后台 AI 理解文本的一期里，**手动置顶**是最轻量、最可靠的上下文管理方式。

---

## 11.5 当前状态卡

### 作用
手动维护：
- 性格关键词
- 所处阶段
- 与其他角色的关系现状

### 存储方式
当前状态卡作为特殊类型的 Item 存储（`item_type = state_card`）。

每个角色只有一个当前状态卡，用户可以随时编辑更新。

### 展示位置
- 在角色页顶部基础信息区下方展示
- 在多角色共现页中，展示所选角色的当前状态卡
- 在创作片段编辑器侧边栏中可快速查看相关角色的当前状态

### 当前定位
有价值，但不作为一期 P0 主闭环依赖。
保留在 P1 中增强"写前扫一眼"的能力。

---

# 12. AI 辅助边界

## 12.1 基本规则
- 默认关闭
- 仅在用户主动点击时触发
- 仅处理用户本次明确提交的内容范围
- 不在后台持续运行
- 不参与模型训练

## 12.2 一期可接受的 AI 场景
仅限用户主动触发时：

- 语义搜索
- 导入时辅助建图
- 公开分享页内容概括
- 摘抄召回

## 12.3 一期不接受的 AI 场景
- AI 判断用户是否 OOC
- AI 自动分析、审查创作内容
- AI 自动续写或生成正文
- AI 后台持续读取当前编辑器内容

---

# 13. 隐私与边界

## 13.1 隐私原则
- 不使用 AI 在后台读取或分析用户私人创作文本
- 不基于用户文本自动判断剧情阶段、关系变化、角色状态
- 不基于用户输入内容做模型训练
- 所有内容组织优先依赖用户手动输入、挂载与排序
- 用户文本仅用于存储、检索、展示与基础管理

## 13.2 产品边界
一期不承诺：
- 理解你的全部剧情
- 理解你的全部关系演化
- 判断你是否 OOC
- 替你推进剧情
- 替你生成创作内容

一期承诺的是：
- 给你一个轻量起步的角色工作台
- 让你能从 0 创建角色
- 让你能通过导入把旧内容带进来
- 让你能围绕同一组角色继续创作
- 让多人剧情的时间衔接更顺

---

# 14. 优先级分层

## 14.1 P0：一期核心闭环
以下能力构成一期成立的主骨架（✅ 已全部完成）：

1. ✅ 首页（角色库，导入为主、新建角色为辅）
2. ✅ 新建角色（仅角色名必填，立即进入角色页）
3. ✅ 导入内容（两种模式：结构化内容导入 + AI 剧情解析导入）
4. ✅ 角色页（资料 / 创作分区，头像上传，备注内联编辑）
5. ✅ 新增内容项（支持图片上传，多种类型）
6. ✅ 多角色共现页（支持 2 个及以上角色）
7. ✅ 共现时间轴（拖拽排序 + 阶段标签）
8. ✅ 基础搜索
9. ✅ 手动置顶

---

## 14.2 P1：一期增强
这些能力不从方案中删除，但不作为第一波主开发依赖：

1. 单角色时间轴
2. 当前状态卡
3. 角色公开分享页
4. AI 语义搜索（主动触发）
5. AI 导入辅助建图（主动触发）
6. AI 摘抄召回（主动触发）
7. AI 公开页概括（主动触发）
8. 角色组保存与快速切换
9. Item 叙事分支标签（`branch` 字段）：标记 IF线/AU/平行线等非主线内容；`null` 为主线，非 null 字符串为分支名（如 `"if线-A与B相遇"`）；UI 上以浅色 badge 展示，共现页和时间轴支持按分支过滤

### AI 摘抄召回触发方式说明
用户在创作片段编辑器中写作时，可以主动点击"召回相关摘抄"按钮。

系统将用户当前选中的段落或正在编辑的段落提交给 AI，AI 返回相关的摘抄建议。

不在后台自动分析用户正在输入的文本，完全由用户主动触发。

---

## 14.3 P2：后续迭代
这些方向保留为后续扩展，不从方案中砍掉：

1. 关系图谱
2. 分层时间轴
3. 剧情线对象
4. 世界观对象
5. 更细的内容类型
6. 更丰富的筛选器
7. 非侵入式整理辅助
8. 更复杂的关系状态管理
9. 角色阶段快照系统

---

# 15. 一期中的“创作辅助”体现在哪里

虽然一期不依赖后台 AI 自动理解文本，但它仍然是创作辅助工具，因为它提供了以下能力：

## 15.1 从 0 到继续写的路径是连贯的
创建角色不是为了填档案，而是为了尽快进入可持续写作状态。

## 15.2 导入不是仓库，而是进入工作台的入口
旧内容导入后直接挂到角色和角色组合上，能立刻参与后续创作。

## 15.3 多角色共现是主创作场景
不是只围绕单角色记笔记，而是围绕“这群人一起发生了什么”继续写。

## 15.4 共现时间轴服务剧情衔接
帮助用户在多人剧情中保持前后顺序和上下文连续性。

## 15.5 手动置顶替代后台理解
通过显式置顶关键内容，用户可以自己定义当前最重要的上下文。

---

# 16. MVP 成功标准与指标

## 16.1 成功指标体系

### 领先指标（上线后 1-4 周内观察）

**冷启动效率**
- 目标：80% 的新用户能在 5 分钟内创建第一个角色
- 测量方式：从注册到第一个角色创建完成的时间中位数
- 成功阈值：中位数 ≤ 5 分钟
- 拉伸目标：中位数 ≤ 3 分钟

**首次内容添加率**
- 目标：70% 的创建角色的用户会在当天添加至少 1 条内容
- 测量方式：创建角色后 24 小时内添加 Item 的用户占比
- 成功阈值：≥ 70%
- 拉伸目标：≥ 80%

**导入功能采用率**
- 目标：40% 的新用户会在首周使用导入功能
- 测量方式：注册后 7 天内使用过导入功能的用户占比
- 成功阈值：≥ 40%
- 拉伸目标：≥ 50%

**多角色共现页使用率**
- 目标：60% 的活跃用户会在首周进入多角色共现页
- 测量方式：7 天内至少访问过 1 次多角色共现页的用户占比
- 成功阈值：≥ 60%
- 拉伸目标：≥ 70%

**时间轴交互率**
- 目标：50% 的使用时间轴的用户会进行排序或打标签操作
- 测量方式：访问时间轴页面且进行过拖拽排序或添加阶段标签的用户占比
- 成功阈值：≥ 50%
- 拉伸目标：≥ 60%

### 滞后指标（上线后 4-12 周观察）

**7 日留存率**
- 目标：新用户 7 日留存率 ≥ 40%
- 测量方式：注册后第 7 天仍有活跃行为的用户占比
- 成功阈值：≥ 40%
- 拉伸目标：≥ 50%

**内容积累深度**
- 目标：活跃用户平均每个角色挂载 ≥ 10 条内容
- 测量方式：30 天内活跃用户的人均角色内容数
- 成功阈值：≥ 10 条/角色
- 拉伸目标：≥ 15 条/角色

**创作持续性**
- 目标：60% 的活跃用户会在 30 天内新增至少 3 条创作片段
- 测量方式：30 天内新增 ≥ 3 条 snippet 类型 Item 的用户占比
- 成功阈值：≥ 60%
- 拉伸目标：≥ 70%

**工具替代成功率**
- 目标：50% 的用户表示减少了在其他工具中记录 OC 内容的频率
- 测量方式：30 天后用户调研问卷
- 成功阈值：≥ 50% 用户选择"明显减少"或"完全替代"
- 拉伸目标：≥ 60%

## 16.2 定性成功标准

### 冷启动是否足够轻
判断标准：
- 用户能否很快创建一个角色
- 创建后是否愿意继续添加第一批内容
- 是否不会因为字段太多而放弃

### 导入是否真的成为开门能力
判断标准：
- 用户是否愿意先用导入进入产品
- 导入后是否能顺利进入角色页或���角色共现页
- 导入内容是否能在后续创作中被回看和继续使用

### 多角色共现是否真的承接创作
判断标准：
- 用户是否会反复进入多角色共现页
- 用户是否会在共现页中新增创作片段
- 用户是否能围绕同一组角色稳定继续写

### 共现时间轴是否对剧情衔接有价值
判断标准：
- 用户是否会主动调整虚构顺序
- 用户是否会打阶段标签
- 用户是否会通过时间轴回看后继续写

---

# 17. 内容导入设计

## 17.1 设计原则
粗导入，以后再整理。  
不要求用户提前整理格式，降低迁移门槛。

## 17.2 首次使用引导顺序
**实际实现（P0）**：用户直接进入导入页，选择 Tab（内容导入 / 剧情导入），无需提前选角色。导入完成后可进入角色页或多角色共现页。

原规划的"先选角色再导入"流程在 P0 中未实现，改为在导入格式中内联指定角色（内容导入 Tab）或由 AI 自动识别并创建角色（剧情导入 Tab）。

## 17.3 导入方式
- Markdown 文件
- 纯文本
- Word 内容直接粘贴
- 已完成长篇内容的整段导入

## 17.4 导入结果

### 模式 A：内容导入（结构化，默认）
格式：每行 `角色A,角色B\t内容`，Tab 分隔角色列表与内容。
批量解析，按行创建 `snippet` 类型 Item，自动关联指定角色。
角色不存在则自动创建。

### 模式 B：剧情导入（AI 解析，主动触发）
**实际实现（P0，已完成）**：一次性 AI 调用，无确认步骤。

流程：
1. 用户粘贴自由格式剧情文本
2. 后端将全文发给 OpenRouter（DeepSeek V3.2）
3. AI 返回 JSON：`{ characters: [...], snippets: [...] }`
4. 系统自动创建角色（已有则复用）和 `snippet` 类型 Item
5. 完成提示，用户可进入角色页查看结果

> 原规划的 4 步确认流程（确认角色 → 确认关系 → 确认事件 → 确认虚构顺序）在 P0 中未实现，以一次性写入替代，降低实现复杂度。P1 可按需补充确认流程。

### 原规划：AI 辅助导入确认流程（P0 未实现，保留���参考）

采用分步骤引导模式：

**步骤 1：确认角色**
- AI 分析导入内容，识别出现的角色名
- 展示角色候选列表
- 用户逐个确认：创建新角色 / 关联到已有角色 / 忽略
- 用户可以手动修改角色名

**步骤 2：确认关系**
- AI 提取角色之间的关系描述
- 展示关系候选列���（如"A 认为 B 是朋友"）
- 用户逐条确认：接受 / 修改 / 忽略
- 确认的关系描述会作为 profile 类型 Item 挂到对应角色下

**步骤 3：确认事件**
- AI 识别关键事件段落
- 展示事件候选列表，���个事件包含：
  - 事件内容片段
  - 建议的关联角色
  - 建议的内容类型（snippet / profile / reference）
- 用户逐条确认：接受 / 修改 / 忽略

**步骤 4：确认虚构顺序**
- AI 根据内容推断事件的虚���时间顺序
- 展示时间轴预览，用户可以：
  - 拖拽调整顺序
  - 添加或修改阶段标签
  - 确认后写入系统

**完成**
- 所有确认的内容写入系统
- 自动进入多角色共现页或角色页，用户可以立即开始继续创作

## 17.5 后续整理入口
用户回看导入内容时，可以：
- 拆分为新条目
- 修改 Item 类型
- 补角色标签
- 调整虚构顺序
- 添加阶段标签

### 内容拆分交互细节

**入口位置**：内容详情页的操作菜单中提供"拆分内容"选项

**拆分流程**：
1. 用户点击"拆分内容"
2. 进入拆分编辑模式，用户可以：
   - 选择要拆分出的文本段落
   - 为每个新条目选择内容类型（profile / snippet / reference / image）
   - 预览拆分结果
3. 用户确认拆分
4. 系统询问：是否保留原条目？
   - 保留：原条目继续存在，新条目作为独立内容创建
   - 删除：原条目被删除，仅保留拆分后的新条目
5. 新拆分的条目自动继承原条目的：
   - 角色标签（`linked_characters`）
   - 虚构顺序（`fictional_order`）
   - 虚构阶段标签（`fictional_stage`）
6. 用户可以在拆分后立即修改这些继承的属性

---

# 18. 完整使用流程

## 场景 1：从零开始
1. 创建角色
2. 进入角色页
3. 添加 profile / snippet / reference / image
4. 继续写新片段
5. 邀请其他角色进入共现页
6. 在共现时间轴中梳理共同经历

## 场景 2：导入旧内容继续创作
1. 进入首页，点击导入
2. 先选择角色 / 多个角色
3. 粘贴或上传旧内容
4. 系统原样存为 reference
5. 进入角色页或共现页
6. 按需拆分、补标签、排序
7. 继续写新片段

## 场景 3：梳理同一世界观下的多人剧情
1. 选择多个角色
2. 进入多角色共现页
3. 查看共同关联内容
4. 切到共现时间轴
5. 调整顺序、打阶段标签
6. 在此基础上继续写新剧情片段

---

# 19. 一句话定义一期 MVP

**一期 MVP 是一个围绕角色与多角色共现创作场景搭建的 OC 创作工作台：既支持用户从 0 创建角色并逐步挂载内容，也支持用户通过导入把旧内容带入系统，并通过共现时间轴继续推进同一世界观下的多人剧情创作。**

---

# 20. P0 功能验收标准

## 20.1 首页 / 角色库

### AC-001: 导入内容按钮
- Given 用户进入首页
- When 用户查看页面
- Then 导入内容按钮应显示在主要位置（主按钮样式）
- And 新建角色按钮显示在次要位置（次按钮样式）

### AC-002: 角色列表展示
- Given 用户已创建至少 1 个角色
- When 用户进入首页
- Then 应显示所有已创建角色的列表
- And 每个角色显示：角色名、头像（如有）、一句话备注（如有）
- And 点击角色卡片可进入该角色页

### AC-003: 最近更新展示
- Given 用户有内容更新记录
- When 用户进入首页
- Then 应显示最近更新的角色（按更新时间倒序，最多显示 5 个）
- And 应显示最近更新的内容（按更新时间倒序，最多显示 10 条）
- And 点击内容可直接跳转到该内容详情页

### AC-004: 搜索功能
- Given 用户在首页
- When 用户在搜索框输入关键词
- Then 应实时显示匹配的角色和内容
- And 搜索结果应区分角色和内容两个分类
- And 点击搜索结果可跳转到对应页面

## 20.2 新建角色

### AC-005: 最小化创建流程
- Given 用户点击"新建角色"按钮
- When 创建角色弹窗/页面打开
- Then 只有"角色名"字段标记为必填
- And "一句话备注"和"头像"字段标记为选填
- And 不应出现其他必填字段

### AC-006: 创建成功跳转
- Given 用户填写了角色名
- When 用户点击"创建"按钮
- Then 角色创建成功
- And 自动跳转到该角色的角色页
- And 角色页处于可编辑状态，用户可立即添加内容

### AC-007: 创建失败提示
- Given 用户未填写角色名
- When 用户点击"创建"按钮
- Then 显示错误提示"请输入角色名"
- And 不创建角色，保持在创建页面

## 20.3 导入内容

### AC-008: 导入前选择角色
- Given 用户点击"导入内容"按钮
- When 导入流程开始
- Then 首先显示角色选择界面
- And 用户可以选择 1 个或多个已有角色
- And 用户可以选择"创建新角色"
- And 未选择角色时不能进入下一步

### AC-009: 支持多种导入方式
- Given 用户已选择角色归属
- When 用户进入导入内容步骤
- Then 应支持以下导入方式：
  - [ ] 上传 Markdown 文件
  - [ ] 上传纯文本文件
  - [ ] 直接粘贴文本内容
  - [ ] 上传 Word 文档（.docx）

### AC-010: 简单导入为默认模式
- Given 用户完成内容导入
- When 系统处理导入内容
- Then 内容应原样保留，不做自动拆分
- And 内容类型默认设置为 `reference`
- And 内容自动关联到用户选择的角色
- And 不要求用户立即进行拆分或分类

### AC-011: 导入后跳转
- Given 导入内容处理完成
- When 系统保存成功
- Then 如果关联了 1 个角色，跳转到该角色页
- And 如果关联了多个角色，跳转到多角色共现页
- And 新导入的内容应在页面中可见

### AC-012: AI 辅助导入入口
- Given 用户在导入内容页面
- When 用户查看导入选项
- Then 应显示"使用 AI 辅助建图"选项（非默认）
- And 该选项应清晰标注为"可选"
- And 点击后进入 AI 辅助导入流程

## 20.4 角色页

### AC-013: 角色基础信息展示
- Given 用户进入角色页
- When 页面加载完成
- Then 顶部应显示：
  - [ ] 角色名
  - [ ] 一句话备注（如有）
  - [ ] 角色头像/图片（如有）
  - [ ] "编辑角色"按钮

### AC-014: 资料与创作分 Tab 展示
- Given 用户在角色页
- When 用户查看内容区域
- Then 应显示两个主要 Tab：
  - [ ] "资料" Tab：显示 profile、reference、image 类型内容
  - [ ] "创作" Tab：显示 snippet 类型内容
- And 默认显示"资料" Tab
- And Tab 切换应保持在当前角色页，不刷新页面

### AC-015: 新增内容入口
- Given 用户在角色页
- When 用户查看操作区域
- Then 应提供以下操作入口：
  - [ ] 新增资料
  - [ ] 新增创作片段
  - [ ] 上传图片
  - [ ] 搜索当前角色下内容
  - [ ] 进入多角色共现页

### AC-016: 内容列表展示
- Given 角色下有内容
- When 用户在对应 Tab 中查看
- Then 内容应按最近更新时间倒序排列
- And 置顶内容应显示在列表顶部
- And 每条内容显示：标题（如有）、内容预览、更新时间
- And 点击内容可进入内容详情页

### AC-017: 手动置顶功能
- Given 用户在角色页查看内容列表
- When 用户对某条内容进行置顶操作
- Then 该内容应移动到列表顶部
- And 显示置顶标识
- And 置顶内容按置顶时间倒序排列

## 20.5 多角色共现页

### AC-018: 进入多角色共现页
- Given 用户在角色页
- When 用户点击"与其他角色一起查看"
- Then 显示角色选择器
- And 用户可以多选其他角色（至少选择 1 个，使总数 ≥ 2）
- And 确认后进入多角色共现页

### AC-019: 共现内容展示
- Given 用户选择了 2 个及以上角色
- When 用户进入多角色共现页
- Then 应显示同时关联所选所有角色的内容
- And 内容按最近更新时间倒序排列
- And 每条内容显示关联的所有角色标签

### AC-020: 切换到时间轴视图
- Given 用户在多角色共现页
- When 用户点击"切换到时间轴"
- Then 应切换到共现时间轴视图
- And 内容按虚构顺序（fictional_order）排列
- And 保持当前选择的角色集合不变

### AC-021: 新增内容自动关联
- Given 用户在多角色共现页
- When 用户点击"新增创作片段"
- Then 新内容应自动关联到当前所选的所有角色
- And 用户可以在创建时修改关联的角色
- And 保存后内容应出现在当前共现页中

## 20.6 共现时间轴

### AC-022: 时间轴视图切换
- Given 用户在时间轴页面
- When 用户通过角色选择器改变选择
- Then 选择 1 个角色时，显示单角色时间轴
- And 选择 2+ 个角色时，显示共现时间轴
- And 视图切换应在同一页面完成，不跳��

### AC-023: 虚构顺序展示
- Given 用户在时间轴视图
- When 页面加载完成
- Then 内容应按 fictional_order 字段值排序
- And fictional_order 值小的排在前面
- And 未设置 fictional_order 的内容排在最后

### AC-024: 拖拽排序功能
- Given 用户在时间轴视图
- When 用户拖动某条内容到新位置
- Then 内容应移动到新位置
- And 系统应自动更新该内容及相关内容的 fictional_order 值
- And 排序结果应立即保存

### AC-025: 阶段标签功能
- Given 用户在时间轴视图查看某条内容
- When 用户为内容添加或修改阶段标签
- Then 用户可以输入自定义文本（如"早期""转折后"）
- And 保存后，内容应显示该阶段标签
- And 时间轴可以按阶段标签分组显示（可选视图）

## 20.7 新增内容项

### AC-026: 最小化输入要求
- Given 用户点击"新增资料"或"新增创作片段"
- When 新增内容表单打开
- Then 必填字段只有：
  - [ ] 内容正文
  - [ ] 关联角色（如果从角色页/共现页进入则自动填充）
- And 选填字段包括：
  - [ ] 标题
  - [ ] 内容类型（默认值：资料入口默认 profile，创作入口默认 snippet）
  - [ ] 图片附件

### AC-027: 内容保存成功
- Given 用户填写了必填字段
- When 用户点击"保存"
- Then 内容创建成功
- And 返回到之前的页面（角色页/共现页）
- And 新内容应在列表中可见

## 20.8 基础搜索

### AC-028: 全局搜索
- Given 用户在任意页面
- When 用户在全局搜索框输入关键词
- Then 应搜索所有角色名和内容
- And 实时显示搜索结果
- And 结果按相关度排序

### AC-029: 角色页内搜索
- Given 用户在角色页
- When 用户在角色页搜索框输入关键词
- Then 只搜索当前角色下的内容
- And 实时显示搜索结果
- And 高亮匹配的关键词

### AC-030: 共现页内搜索
- Given 用户在多角色共现页
- When 用户在搜索框输入关键词
- Then 只搜索当前所选角色集合相关的内容
- And 实时显示搜索结果
- And 高亮匹配的关键词

## 20.9 手动置顶

### AC-031: 置顶操作
- Given 用户查看内容列表
- When 用户对某条内容执行置顶操作
- Then 该内容应移动到列表顶部
- And 显示置顶图标或标识
- And 置顶状态应持久化保存

### AC-032: 取消置顶
- Given 某条内容已置顶
- When 用户对该内容执行取消置顶操作
- Then 该内容应回到按更新时间排序的正常位置
- And 置顶标识消失

### AC-033: 多条置顶排序
- Given 用户置顶了多条内容
- When 用户查看��表
- Then 所有置顶内容应显示在列表顶部
- And 置顶内容之间按置顶时间倒序排列（最近置顶的在最上）

---

# 21. 技术约束与依赖

## 21.1 技术栈建议

### 前端
- 框架：React（Web 端优先）
- 富文本编辑器：支持基础 Markdown 的轻量编辑器（如 TipTap / Milkdown）
- 拖拽排序：支持列表拖拽的库（如 dnd-kit）
- 状态管理：轻量方案（Zustand / Jotai）

### 后端
- API：RESTful 或 GraphQL
- 数据库：关系型数据库（PostgreSQL 推荐），支持 JSON 字段存储灵活内容
- 文件存储：图片与文件附件使用对象存储（S3 兼容）
- 搜索：一期使用数据库全文检索，P1 语义搜索可接入向量数据库

### 实际选型（P0 已落地）
- 框架：Next.js（App Router）+ Tailwind CSS + shadcn/ui
- ORM：Prisma 7 + PostgreSQL（Prisma Accelerate 托管，`prisma+postgres://` 连接串）
- 图片存储：base64 data URL 直接存入数据库（暂代对象存储，生产环境可换 S3）
- AI：OpenRouter API（DeepSeek V3.2），key 存于 `ANTHROPIC_API_KEY` 环境变量
- 部署：Vercel

## 21.2 性能约束

- 首页加载：角色列表首屏渲染 ≤ 2s（P95）
- 搜索响应：关键词搜索结果返回 ≤ 500ms
- 导入处理：单次导入文件 ≤ 5MB，处理完成 ≤ 10s
- 拖拽排序：操作反馈 ≤ 100ms，后台持久化异步完成

## 21.3 兼容性约束

- 浏览器：Chrome / Safari / Firefox 最新两个主版本
- 设备：桌面端优先，移动端一期不作为主要适配目标
- 文件导入：支持 .md、.txt、.docx 格式

## 21.4 安全与隐私约束

- 用户数据隔离：每个用户只能访问自己的角色和内容
- 文件上传：需做文件类型校验和大小限制
- AI 调用：仅在用户主动触发时发起，不缓存用户原始文本到 AI 服务

---

# 22. 开放问题

## 22.1 产品层面

| # | 问题 | 影响范围 | 优先级 |
|---|------|----------|--------|
| P-01 | 多角色共现页的角色上限是否需要设置？（如最多同时选 10 个角色） | 共现页性能与 UX | 中 |
| P-02 | 导入内容是否支持批量导入多个文件？ | 导入页设计 | 低 |
| P-03 | 角色删除后，关联的 Item 如何处理？（保留孤立 / 级联删除） | 数据一致性 | 高 |
| P-04 | 是否支持内容的跨角色移动（将某条 Item 从角色 A 移到角色 B）？ | 内容管理 | 中 |

## 22.2 设计层面

| # | 问题 | 影响范围 | 优先级 |
|---|------|----------|--------|
| D-01 | 时间轴中未设置 fictional_order 的内容，排在最后还是单独分区展示？ | 时间轴 UX | 中 |
| D-02 | 多角色共现页中，内容卡片是否需要显示"仅关联部分所选角色"的提示？ | 共现页 UX | 低 |
| D-03 | 新增内容时，内容类型选择器的默认值逻辑是否需要更细化？ | 新增内容表单 | 低 |

## 22.3 工程层面

| # | 问题 | 影响范围 | 优先级 |
|---|------|----------|--------|
| E-01 | fictional_order 的数值分配策略（整数间隔 / 浮点数 / 重新编号）？ | 拖拽排序实现 | 高 |
| E-02 | 全文搜索是否需要支持中文分词？ | 搜索质量 | 高 |
| E-03 | 图片存储是否需要生成缩略图？ | 性能与存储成本 | 中 |

---

# 23. 风险与缓解措施

| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|----------|
| 冷启动门槛仍然过高，用户创建角色后不继续添加内容 | 中 | 高 | 创建角色后提供引导提示；角色页空状态设计友好 |
| 导入内容后用户不知道如何继续使用 | 中 | 高 | 导入完成后提供明确的"下一步"引导；空状态文案优化 |
| 多角色共现页内容过多导致信息过载 | 中 | 中 | 默认按最近更新排序；支持按类型筛选；支持搜索 |
| 拖拽排序在移动端体验差 | 高 | 低 | 一期桌面端优先，移动端拖拽降级为手动输入顺序值 |
| AI 辅助导入识别准确率低导致用户信任度下降 | 中 | 中 | 所有 AI 结果均需用户逐条确认，不自动写入 |
| fictional_order 并发修改冲突 | 低 | 中 | 乐观锁 + 冲突时提示用户刷新 |

---

# 24. 后续迭代方向

## 24.1 P1 优先级排序建议

基于用户价值与实现成本综合评估：

1. **当前状态卡**（state_card）— 写前扫一眼，高频刚需
2. **角色组保存与快速切换** — 减少重复选角色操作
3. **单角色时间轴** — 补全时间轴能力
4. **AI 语义搜索** — 提升内容召回效率
5. **AI 摘抄召回** — 写作时的上下文辅助
6. **角色公开分享页** — 约稿场景支持
7. **AI 导入辅助建图** — 降低旧内容迁移成本
8. **AI 公开页概括** — 分享场景增强

## 24.2 P2 方向说明

| 方向 | 说明 | 前置依赖 |
|------|------|----------|
| 关系图谱 | 可视化角色关系网络，支持双向视角 | P0 角色与 Item 数据积累 |
| 分层时间轴 | 以叙事线为纵轴的全局剧情视图 | NarrativeLine 对象实现 |
| 剧情线对象 | 管理多条并行剧情线，Item 可关联多条线 | P0 数据模型稳定 |
| 角色阶段快照 | 记录角色在特定时间点的状态快照 | state_card P1 完成 |
| 非侵入式整理辅助 | AI 在用户查看内容时提供可选整理建议 | 用户信任度建立后 |